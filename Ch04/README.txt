posts.go is the first example.

spam.go is the second example, to run it, must put the email directory to the same directory.

feeds.go is the third example, to run it you need install github.com/jteeuwen/go-pkg-rss for pasrsing rss feed.

